{
  "name": "train-ui",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "install:all": "npm ci --prefix web && npm ci --prefix admin",
    "dev:web": "npm run web-dev --prefix web",
    "dev:admin": "npm run admin-dev --prefix admin",
    "build": "npm run build --prefix web && npm run build --prefix admin",
    "build:pages": "npm run build:pages --prefix web && npm run build:pages --prefix admin && node scripts/stage-pages.mjs",
    "lint": "npm run lint --prefix web && npm run lint --prefix admin"
  }
}
